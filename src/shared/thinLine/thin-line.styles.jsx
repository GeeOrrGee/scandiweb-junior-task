import styled from 'styled-components'

export const ThinGreyLine = styled.div`
  width: 100%;
  background-color: rgb(128, 128, 128, 0.3);
  height: 1px;
  /* ${({ top = 0, left = 0 }) => `
  top: ${top}%;
  left: ${left}%;
  `} */
`
